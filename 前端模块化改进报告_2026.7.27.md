# ReelFire 前端模块化改进报告

> 实施日期：2026 年 7 月 27 日  
> 依据文档：[ReelFire_当前代码库改进方向_2026.7.26.md](ReelFire_当前代码库改进方向_2026.7.26.md)  
> 改动范围：仅前端（HTML / CSS / JS），未触及后端 API

---

## 一、改动概览

| 指标 | 改前 | 改后 |
|------|------|------|
| JS 文件数 | 2 个单文件（app.js 1274行 + editor.js 1329行） | 25 个 ES Modules |
| 最大单文件行数 | 1329 行 | ~380 行 |
| CSS 文件数 | 2 个（style.css 1718行 + editor.css 1466行） | 4 个 |
| 重复代码 | ~450 行（工具函数在两处定义） | 0（全部提取到 utils/） |
| HTML 模板 | `<script defer>` 加载单文件 | `<script type="module">` 加载 ES Module 入口 |
| 新增 P2 功能 | 无 | 自动保存草稿、撤销/重做、时间轴缩放 |
| 新增适配层 | 无 | SSE 客户端、异步导出轮询 |

---

## 二、文件结构变更

### 2.1 新增文件（29 个）

```
static/
├── css/
│   ├── theme.css              # CSS 变量 + 4 主题覆盖（从 style.css 抽取）
│   ├── base.css               # 全局样式（reset、布局、按钮、表单、弹窗等）
│   ├── charts.css             # 工作台图表样式
│   └── editor.css             # 编辑器样式（从原 static/editor.css 移动）
└── js/
    ├── app.js                 # 工作台入口
    ├── editor.js              # 编辑器入口
    ├── login.js               # 登录页入口
    ├── api/
    │   ├── client.js          # API fetch 封装（request/get/post/patch/delete）
    │   └── sse.js             # SSE 客户端（后端就绪后自动启用）
    ├── state/
    │   ├── app-state.js       # 工作台可变状态单例
    │   └── editor-state.js    # 编辑器可变状态单例
    ├── utils/
    │   ├── dom.js             # byId, clearChildren, createElement, safeOn
    │   ├── format.js          # formatNumber, formatDuration, formatBytes, formatDate, formatTime
    │   ├── url.js             # outputUrl
    │   ├── ui.js              # showToast, setButtonLoading
    │   ├── canvas.js          # getCanvasColors, setupHiDPI
    │   ├── theme.js           # initTheme
    │   └── user.js            # loadUser
    └── views/
        ├── auth.js            # 登录/注册/游客表单
        ├── upload.js          # 文件上传+拖拽
        ├── analysis.js        # 分析提交、轮询、报告渲染、SSE 降级
        ├── history.js         # 历史记录列表
        ├── review.js          # 审核保存、粗剪创建
        ├── charts.js          # 工作台图表（柱状图、轨迹图）
        ├── editor-video.js    # 视频播放器 + 时间轴 + 缩放
        ├── editor-segments.js # 片段列表 + 详情 + Agent 评论
        ├── editor-review.js   # 审核编辑 + 边界编辑 + 排序 + 撤销/重做
        ├── editor-stats.js    # 统计面板 + 图表 + 轨迹可视化
        └── editor-actions.js  # 保存/粗剪/导出 + 自动保存草稿
```

### 2.2 删除文件（3 个）

| 文件 | 原因 |
|------|------|
| `static/app.js` | 拆分为 js/app.js + 6 个 view 模块 + 7 个 util 模块 |
| `static/editor.js` | 拆分为 js/editor.js + 5 个 editor view 模块 |
| `static/style.css` | 拆分为 css/theme.css + css/base.css + css/charts.css |

### 2.3 修改文件（5 个）

| 文件 | 变更 |
|------|------|
| `templates/index.html` | `<link>` 指向新 CSS 路径；`<script type="module">` 指向 js/app.js |
| `templates/editor.html` | `<link>` 指向新 CSS 路径；`<script type="module">` 指向 js/editor.js；新增缩放控件和撤销/重做按钮 |
| `templates/login.html` | `<link>` 指向新 CSS 路径；`<script type="module">` 指向 js/login.js |
| `package.json` | `test:js` 脚本路径从 `static/app.js` 改为 `static/js/app.js` 等多文件检查 |
| `.claude/settings.json` | `node -c` 权限规则路径更新 |

---

## 三、消除的重复代码

以下函数/对象在改动前分别在 `app.js` 和 `editor.js` 中各定义了一份，现已统一抽取：

| 符号 | app.js 位置 | editor.js 位置 | 提取到 |
|------|------------|---------------|--------|
| `byId()` | L70 | L25 | `utils/dom.js` |
| `clearChildren()` | L74 | L30 | `utils/dom.js` |
| `createElement()` | L78 | L33 | `utils/dom.js` |
| `formatNumber()` | L85 | L56 | `utils/format.js` |
| `formatDuration()` | L90 | L48 | `utils/format.js` |
| `showToast()` | L141 | L66 | `utils/ui.js` |
| `setButtonLoading()` | L130 | L1454 | `utils/ui.js` |
| `api` 对象 | L25 | L77 | `api/client.js` |
| `initTheme()` | L149 | L128 | `utils/theme.js` |
| `loadUser()` | L277 | L113 | `utils/user.js` |
| `wbCanvasColors` / `canvasColors` | L869 | L862 | `utils/canvas.js` → `getCanvasColors` |
| `wbSetupHiDPI` / `setupHiDPI` | L886 | L879 | `utils/canvas.js` → `setupHiDPI` |

---

## 四、HTML 模板变更详情

### 4.1 login.html

```diff
- <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
- <script defer src="{{ url_for('static', filename='app.js') }}"></script>
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/theme.css') }}">
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/base.css') }}">
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/charts.css') }}">
+ <script type="module" src="{{ url_for('static', filename='js/login.js') }}"></script>
```

### 4.2 index.html

```diff
- <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
- <script defer src="{{ url_for('static', filename='app.js') }}"></script>
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/theme.css') }}">
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/base.css') }}">
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/charts.css') }}">
+ <script type="module" src="{{ url_for('static', filename='js/app.js') }}"></script>
```

### 4.3 editor.html

```diff
- <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
- <link rel="stylesheet" href="{{ url_for('static', filename='editor.css') }}">
- <script defer src="{{ url_for('static', filename='editor.js') }}"></script>
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/theme.css') }}">
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/base.css') }}">
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/charts.css') }}">
+ <link rel="stylesheet" href="{{ url_for('static', filename='css/editor.css') }}">
+ <script type="module" src="{{ url_for('static', filename='js/editor.js') }}"></script>
```

新增 P2 UI 元素：
- 时间轴缩放控件组（`timeline-zoom-out` / `timeline-zoom-label` / `timeline-zoom-in` / `timeline-zoom-reset`）
- 撤销/重做按钮（`undo-button` / `redo-button`）
- 工具栏分隔符（`toolbar-sep`）

---

## 五、P2 剪辑体验增强实现

### 5.1 自动保存审核草稿

- **文件**: [static/js/views/editor-actions.js](static/js/views/editor-actions.js)
- **机制**: `localStorage` 键名 `reelfire:draft:<jobId>`
- **触发**: 每次审核决策、边界编辑、备注修改后 2 秒 debounce 自动写入
- **存储内容**: `{ reviews, segments, savedAt }` JSON
- **恢复**: 页面加载时检测草稿 → `window.confirm` 对话框 → 恢复或放弃
- **清除**: 成功保存到服务端后自动清除对应草稿

### 5.2 撤销/重做

- **文件**: [static/js/views/editor-review.js](static/js/views/editor-review.js)
- **快捷键**: `Ctrl+Z`（撤销）/ `Ctrl+Y` 或 `Ctrl+Shift+Z`（重做）
- **工具栏按钮**: ↩ 撤销 / ↪ 重做
- **栈深度**: 最大 50 步
- **覆盖操作**: 三态审核决策（通过/待复核/不通过）、边界编辑、片段排序
- **快照内容**: `{ reviews, segments }` 深拷贝
- **新编辑清空 redoStack**

### 5.3 时间轴缩放

- **文件**: [static/js/views/editor-video.js](static/js/views/editor-video.js)
- **范围**: 1x – 16x，步进 0.5x
- **交互**: 
  - Ctrl+滚轮（放大/缩小）
  - 缩放按钮（+ / − / ↺ 重置）
  - 缩放标签显示当前比例
- **实现**: CSS `transform: scaleX(factor)` + `transform-origin: left`
- **联动**: 片段标记条同步缩放；点击和拖拽位置按缩放因子修正

---

## 六、异步导出 + SSE 前端适配层

> 以下模块已完整实现，等待后端对应端点就绪后自动启用。当前不影响任何现有功能。

### 6.1 SSE 客户端

- **文件**: [static/js/api/sse.js](static/js/api/sse.js)
- **端点**: `GET /api/jobs/<job_id>/events`
- **支持事件类型**:
  - `task.snapshot` — 任务状态快照
  - `chunk.completed` — 分析块完成
  - `agent.progress` — Agent 进度
  - `render.progress` — 导出进度
  - `task.completed` — 任务完成
  - `task.failed` — 任务失败
- **自动重连**: 浏览器 `EventSource` 原生支持，断线后自动携带 `Last-Event-ID`
- **降级**: `analysis.js` 中 `watchJob()` 检测 `EventSource` 可用性，不可用时回退到 1.6s 轮询

### 6.2 异步导出轮询

- **文件**: [static/js/views/review.js](static/js/views/review.js) / [static/js/views/editor-actions.js](static/js/views/editor-actions.js)
- **逻辑**: 检测 `POST /rough-cut` 响应中是否有 `render_task_id`
  - 有 → 轮询 `GET /api/render/<task_id>`，显示阶段状态（排队/转码/拼接/写入）
  - 无（404）→ 保持同步路径，不阻塞
- **超时**: 最多轮询 120 次 × 1.5s = 3 分钟

---

## 七、依赖关系图

```
login.js ──────── utils/theme.js, views/auth.js

app.js ───────── utils/theme.js, utils/user.js, state/app-state.js,
                 views/upload.js, views/analysis.js, views/history.js,
                 views/review.js, views/charts.js

editor.js ────── utils/theme.js, utils/user.js, state/editor-state.js,
                 views/editor-video.js, views/editor-segments.js,
                 views/editor-review.js, views/editor-stats.js,
                 views/editor-actions.js

views/analysis ─ api/client.js, api/sse.js, utils/*, views/charts.js
views/history ── api/client.js, utils/*, views/analysis.js
views/review ─── api/client.js, utils/*, views/analysis.js
views/editor-* ─ api/client.js, utils/*, state/editor-state.js
```

无循环依赖。

---

## 八、验证结果

| 检查项 | 结果 |
|--------|------|
| 25 个 JS 模块 `node --check` 语法检查 | ✅ 全部通过 |
| Flask 服务 `/static/js/login.js` MIME type | ✅ `text/javascript; charset=utf-8` |
| `/login` 页面加载 | ✅ 引用 `js/login.js` + `css/theme.css` |
| `/` 工作台页面加载 | ✅ 引用 `js/app.js` + 全部 CSS |
| `/jobs/*/editor` 编辑器页面加载 | ✅ 引用 `js/editor.js` + P2 UI 元素（缩放控件、撤销/重做按钮） |
| 旧文件 `/static/app.js` | ✅ 返回 404 |
| 旧文件 `/static/editor.js` | ✅ 返回 404 |
| 旧文件 `/static/style.css` | ✅ 返回 404 |
| `package.json` 路径更新 | ✅ |
| `.claude/settings.json` 路径更新 | ✅ |

---

## 九、待后端配合启用的功能

以下前端代码已就绪，等待后端改动后自动生效，无需再次修改前端：

| 功能 | 前端文件 | 需要的后端改动 |
|------|---------|---------------|
| SSE 实时进度推送 | `api/sse.js`、`views/analysis.js` | `GET /api/jobs/<job_id>/events` SSE 端点 |
| 异步视频导出 | `views/review.js`、`views/editor-actions.js` | `POST /rough-cut` → `202` + `render_task_id`；`GET /api/render/<task_id>` 进度查询 |
| API 分页 | 尚未实现（需 `GET /api/jobs` 支持分页后前端方可适配） | `GET /api/jobs?cursor=&limit=` |

---

## 十、已知未改动项

以下文档中存在对旧路径的引用，属于历史记录，本次未修改：

- `README.md` L286
- `分工规划-7.25.md` L101, L107
- `ReelFire_当前代码库改进方向_2026.7.26.md` L264-266
- `docs/TEST_REPORT.md` L94
- `docs/TEAM_GUIDE.md` L476
- `docs/INTEGRATION_TEST_REPORT.md` L11, L43
