# ReelFire 8 分钟演示流程

## 1. 演示准备

演示前准备：

- 一段 1～3 分钟、已验证可产生关键帧的 CS:GO/CS2 或 Valorant MP4；
- 一个已经完成的历史任务，用于网络、推理或编码较慢时兜底；
- 一段无音频短视频，用于说明兼容性；
- 16:9 和 9:16 两个已生成输出；
- `job.json`、`analysis_report.json`、关键帧联系表；
- 正常/异常测试报告和两个真实 Bug 记录；
- 浏览器、终端和文件夹窗口提前打开，关闭无关通知。

## 2. 时间安排

| 时间 | 操作 | 讲解重点 | 负责人角色 |
|---|---|---|---|
| 0:00–0:40 | 展示首页和产品名称 | FPS 玩家回看长录像成本高；ReelFire 做机器初选和人工复核 | 组长、产品负责人 |
| 0:40–1:20 | 展示系统架构和职责 | Flask 异步任务、OpenCV/YOLO、FFmpeg、本地 JSON | 组长、产品负责人 |
| 1:20–2:10 | 选择游戏、上传视频并创建任务 | 上传立即返回 `job_id`，页面不阻塞等待 | 前端/后端工程师 |
| 2:10–2:40 | 展示状态轮询 | `queued -> running -> completed/failed` 和失败留档 | 后端工程师 |
| 2:40–3:50 | 打开已完成任务并展示关键帧 | YOLO 检测框、目标/场景变化/运动分数和综合公式 | CV 算法工程师 |
| 3:50–4:30 | 说明关键帧去重和片段边界 | 相邻高分不重复；开头、结尾仍保持合法时长 | CV 算法工程师 |
| 4:30–5:20 | 保留/忽略、排序、标签、备注 | 人工决定最终内容，修改写回 JSON | 前端工程师 |
| 5:20–5:50 | 修改片段开始和结束时间 | 浏览器和后端双重校验非法边界 | 前端/后端工程师 |
| 5:50–6:30 | 生成并播放 16:9 与 9:16 视频 | FFmpeg、音频保留、无音轨兼容 | CV 算法工程师 |
| 6:30–7:00 | 展示联系表、报告和历史任务重开 | 所有结果可追溯、服务重启后可继续审核 | 后端工程师 |
| 7:00–7:35 | 展示异常场景和 Bug 记录 | 空文件、模型缺失或非法边界不会使服务退出 | 测试工程师 |
| 7:35–8:00 | 总结交付物和下一步 | 已完成 MVP；多片段、方形、ZIP 为后续增强 | 组长、产品负责人 |

## 3. 推荐讲解词

### 开场

> FPS 玩家的一局录像可能持续几十分钟，但真正值得分享的交火、残局和连续击杀只占很短时间。ReelFire 通过目标检测、场景变化和运动强度自动推荐候选，再把最终决定权交给玩家。

### 算法

> 系统不是只给出一个不可解释的“精彩”标签。每张采样帧都有目标分数、场景变化分数和运动分数，并按照 0.45、0.35、0.20 的权重生成综合分数。连续高分帧还会按时间间隔去重，避免同一场交火重复出现。

### 人工复核

> 自动推荐只是初选。玩家可以保留或忽略关键帧、调整排序、增加标签和备注，也可以修改片段边界，确保击杀前的铺垫和击杀后的反馈都被保留。

### 交付

> 每个任务都有独立目录、状态文件和分析报告。即使任务失败也会保存错误；完成任务可以从历史列表重新打开，并输出横屏、竖屏视频和关键帧联系表。

## 4. 演示兜底策略

| 问题 | 现场处理 |
|---|---|
| YOLO 推理耗时 | 创建任务后切换到预先完成的历史任务，同时说明异步设计 |
| FFmpeg 临时编码慢 | 展示已生成输出，并保留本次任务继续在后台运行 |
| 演示素材没有高分结果 | 使用预先验证素材，不在现场临时更换未知视频 |
| 浏览器状态异常 | 刷新后从历史任务重新打开，体现持久化能力 |
| GPU 不可用 | 健康接口展示 CPU 回退；演示使用预生成结果 |
| 输出无音频 | 同时展示 ffprobe 音轨检测和无音频兼容测试 |

## 5. 演示前最终检查

- [x] `/api/health` 正常；
- [x] 模型路径正确；
- [x] 演示素材路径正确；
- [x] 历史兜底任务可以打开；
- [x] 两种输出视频可以播放；
- [x] 关键帧和联系表可以打开；
- [x] 报告 JSON 格式正确；
- [x] 浏览器无缓存导致的旧页面；
- [x] 终端不显示隐私路径、账号或密钥；
- [x] 已按角色和 8 分钟流程完成本机计时彩排。

## 6. 2026-07-18 本机彩排记录

| 检查 | 实际结果 |
|---|---|
| 演示主任务 | `20260718_163104_bed0ce69`，有音频正常流程 |
| 异常兜底任务 | `20260718_163203_53a26fda`，损坏视频 `failed` |
| 完整浏览器流程 | 上传、运行、完成、审核、粗剪、失败、历史重开全部通过 |
| 页面质量 | 控制台错误 0，失败请求 0 |
| 状态与结果证据 | `Acceptance_screenshot/01`～`08` |
| 无音频兜底 | 9:16 输出 1080×1920，`TEST_REPORT.md` N-03～N-05 |

彩排按本文件 0:00～8:00 的顺序执行。真实推理在演示前预先完成并保留历史任务；现场仍展示一次创建任务和状态变化，若推理耗时超出分配时段，立即切换已完成任务继续讲解，不等待现场计算。
